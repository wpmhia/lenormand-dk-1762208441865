// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// Placeholder model to prevent Prisma generation errors
// This application doesn't use database features
model Placeholder {
  id    Int     @id @default(autoincrement())
  name  String?
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}